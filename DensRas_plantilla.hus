<!DOCTYPE html><html xmlns="http://www.w3.org/1999/xhtml">
<head>
  <title>DensRas: Densification of rasters by bilinear or bicubic
  interpolation</title>
<meta content="text/html; charset=ISO-8859-1" http-equiv="Content-Type"/></head>

<body>
 <!--Aquí inicia la part corresponent al titol de la MSA. Aquesta part s'ha d'editar desde el document WPS (aplicacio.mps).-->
<h1><span id="wpstitol"/></h1>
<!--Aquí acaba la part corresponent titol de la MSA editable de el WPS.-->
<!--Aquí inicia la part corresponent a les opcions de de la MSA. Aquesta part s'ha d'editar desde el document WPS (aplicacio.mps).-->
<span id="wpsopcions"/>
<!--Aquí acaba la part corresponent a les opcions de de la MSA editable de el WPS.--> 

  <p>This program changes the spatial resolution of a raster by interpolating
  its values. The program reads the documentation file and, if a flag value is
  indicated, it is not considered in the interpolation process. Byte, (short)
  integer and real data types are supported.</p>

  <p>If you wish to densify exactly the same coordinates of the original file
  (by indicating x x x x in the command line) you must keep in mind that in
  the borders a simplification will be applied. Therefore, it is not
  convenient to connect by adjacency the result of the densification with
  another densification. In this case, it is better to have some extra area
  around the region considered as fully reliable. In addition, when you
  densify exactly the same coordinates than the original file, it is neither
  convenient to apply algorithms related to hydrological fluxes; if necessary,
  omit the most exterior concentric area:</p>

  <blockquote>
    If you use bilinear interpolation, eliminate a buffer of 1 pixel all
    around the image, if you use bicubic interpolation eliminate a buffer of 2
    pixels all around the image.
  </blockquote>
  <hr/>

  <exclusiva>
   <!-- Aquí inicia la part corresponent a la sintaxi de miramon. Aquesta part s'ha d'editar desde el document WPS (aplicacio.mps).-->
<span id="wpssintaxi"/>
<!-- Aquí acaba la part corresponent a la sintaxi de miramon editable de el WPS.-->
   <!-- $$EXCLUSIVA$$

To obtain the complete help, please type:
DensRas ?
-->
  </exclusiva>
</body>
</html>