<!DOCTYPE html><html xmlns="http://www.w3.org/1999/xhtml">
<!--/* Rev AA 20-04-2023 */-->
<head>
  <title>Ombra: Calculation of the limit angles that cause projected shadows in a DEM</title>
<meta content="text/html; charset=ISO-8859-1" http-equiv="Content-Type"/></head>

<body>
<!--Aquí inicia la part corresponent al titol de la MSA. Aquesta part s'ha d'editar desde el document WPS (aplicacio.mps).-->
<h1><span id="wpstitol"/></h1>
<!--Aquí acaba la part corresponent titol de la MSA editable de el WPS.-->

<!--Aquí inicia la part corresponent a les opcions de de la MSA. Aquesta part s'ha d'editar desde el document WPS (aplicacio.mps).-->
<span id="wpsopcions"/>
<!--Aquí acaba la part corresponent a les opcions de de la MSA editable de el WPS.--> 

  <p>The solar azimuth is expressed in degrees from the North and clockwise
  (for example 134.97 is approx. SE); <a href="Astres.htm">Astres</a> application can give the user this information for
  any day in the year, at any time in any place on the Earth.</p>

  <p>The DEM may be in byte, (short) integer (with or without sign) or real format, compressed or
  uncompressed.</p>

  
  <!-- Aquest paràmetre ja es llegeix sempre de les metadades i no té sentit
If the parameter "Conv_heights->Map_units" is 'x', the program will try to 
deduce its value from the field "value units" of the DEMFile documentation; for
example, if the reference system units are meters ('m') and the value units 
are decimeters ('dm'), the program will infer that it has to apply a factor 
0.1 to heights if they are to be comparable with the DEM planimetry. If you indicate 
a conversor (for example 1 if both reference system and heights have the same 
units), this value will be used without considering the file documentation.
<P>
-->
  <p>The output file is a raster (byte, or byte-RLE when NoData zones exist) and
  its values are expressed in degrees, being the minimum solar elevation angle
  that illuminates each point when the Sun is in the direction (azimuth) given
  by the program. Therefore, the values in the output file will be in the
  interval [0,90). The palette cShadow256.dbf is especially suitable to see this
  output file.</p>

  <p>If the DEM has NoData values and they are correctly documented, pixels that
  are affected by a NoData value in the input DEM will be treated as
  &quot;transparent&quot; in the horizon and they will be ignored in the
  calculations.</p>

  <p>If the DEM is a multiband raster with pixels of different size for each
  band, the <i>/MULTIBANDA</i> parameter must be used to generate a
  multiband raster with the corresponding shadows models as a result. It
  should be noted that solar position is considered unique for all bands. If
  the geographical extends were excessively separated between them a unique
  execution for all bands with the solar position at the center of the extend
  ought to be done.<br/>
  For the multiband case, the parameter <i>/PREFIX=</i> indicates the prefix
  added to the original file names to determine the names of the calculated
  files for the multiband case. By default this prefix is <i>o_</i>.</p>
  <hr/>

  <exclusiva>
<!-- Aquí inicia la part corresponent a la sintaxi de miramon. Aquesta part s'ha d'editar desde el document WPS (aplicacio.mps).-->
<span id="wpssintaxi"/>
<!-- Aquí acaba la part corresponent a la sintaxi de miramon editable de el WPS.-->
<p><!-- $$EXCLUSIVA$$
To obtain the complete help, please type:
OMBRA ?
--></p>

  </exclusiva>
</body>
</html>